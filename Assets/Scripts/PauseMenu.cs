using UnityEngine;
using UnityEngine.Events;

public class PauseMenu : MonoBehaviour
{
    
    public UnityEvent onReturnToMenu = new UnityEvent();
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
